app1 의 리스트 안에
페이징에 필요한 데이터를 json으로 담고
이를 넘겨볼까?



// app1의 리스트에 들어가야 하는 데이터 
	routineList == 리스트
	pagemanager == 객체


리스트를 커스터마이징 해보자

select max(step) from exr_routine_comment
		where post=16;


// 변화된 줄을 찾아서 UI 반영시키기
    직접 할 줄 알아야 해

바뀌었다는 것을 어떻게 알 수 있을까?
이거 진짜 골치 아프네 ㅋ


alter table exr_notice drop column content2;
alter table exr_notice drop column content1;

alter table exr_notice add column content mediumtext;

