mysql -u bb -p -h bodybuddy.caffkbaukwhx.ap-northeast-2.rds.amazonaws.com --port 3306
use bodybuddy;

<안드로이드 파일 전송 구성>

- 제목
- 작성자
- 내용 - 이미지 파일을 그대로 보낼 것!


// 내가 정의한 뷰를 이미지뷰로 변환할 수 없음
PhotoView cannot be cast to android.widget.ImageView


이미지뷰를 지정하였더니 성공하였다!!!!!


이제 DTO 보내기!!


어차피 DTO 세팅이 들어가야 되니까,
세팅된 DTO를 보내는 게 낫지 않을까?

아니면, 하나의 객체이니까 파라미터만 넘겨도 되고//


근데 그 내용을 디비에 넣으면 이미지 태그로 표현이 되려나?


--// 파일로 보내서 파일로 받기!

2)
멀티파트파일로 받아낸 파일을 서비스 객체에서 가공하기!


안드로이드의 통신은 모두 웹 통신이다!


안드로이드 로그인 구현
입력값 보내기


백단에서 확인하고 응답결과 반환




//
1-1) 데이터 전송하기
session

   @PostMapping("/login")
   @ResponseBody
   public ResponseEntity<Msg> androidLogin(Member member){
      MemberDetail memberDetail = new MemberDetail(member);
      log.info("넘어온 member : " + member);
      log.info("만든 디테일 : "+ memberDetail);
      UsernamePasswordAuthenticationToken test = new UsernamePasswordAuthenticationToken(memberDetail, member.getPassword().getPass(), null);
      SecurityContextHolder.getContext().setAuthentication(authenticationProvider.authenticate(test));
      ResponseEntity entity=new ResponseEntity<Msg>(new Msg("로그인 성공"), HttpStatus.OK);
      return entity;
   }


- 멤버변수에 추가
: 
@Autowired
   private AuthenticationProvider authenticationProvider;



요청주소
RestController

email
password.pass


/auth/login POST

로그인
No mapping found for HTTP request with URI [/auth/rest/login/android] in DispatcherServlet with name 'userServlet'

- 왜 매핑이 안 될까?
디티오 앞에 @RequestBody
컨트롤러 앞에 @RestController

//

<위도경도를 가지고 계산한 값을 어떻게 저장할까?>

DailyWalk

idx
memeber_idx
meter
regdate 






